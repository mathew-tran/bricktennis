extends Node

signal PointUpdate(amount)
signal RewardPoints(amount)
signal PlayerHit
signal UpdatePlayerHealth(amount)
signal AddPlayerHealth()
signal NewRoundStart

signal PlayerDeath
signal BGColorChange(newColor)

signal StageUpdate(level)
signal PublishHighScore(amount)
signal ClearScore
signal PlayerWon
